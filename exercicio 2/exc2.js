let x = parseFloat (prompt("Digite o primeiro número"));
let y = parseFloat (prompt("Digite o segundo número"));

const z = (x + y);

document.write(`O resultado da soma ${x} + ${y} é igual a: ${z}`);